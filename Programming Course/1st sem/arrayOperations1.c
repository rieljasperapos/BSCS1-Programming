#include <stdio.h>

void initializeArray();
void displayArray();
int searchElement();
int insertLast();

int main() {
    int size, count;

    printf("Enter size: ");
    scanf("%d", &size);

    int array[size];
}